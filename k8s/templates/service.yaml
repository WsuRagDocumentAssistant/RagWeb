apiVersion: v1
kind: Service
metadata:
  name: rag-web
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: rag-web
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP